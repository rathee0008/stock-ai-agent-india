# Copy this file to .streamlit/secrets.toml for local dev (git-ignored),
# or paste these into the "Secrets" box in Streamlit Community Cloud's app settings.
# Never commit real keys.

ANTHROPIC_API_KEY = "sk-ant-..."
